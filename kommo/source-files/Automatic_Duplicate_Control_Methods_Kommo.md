# Automatic Duplicate Control Methods | Kommo

hello and welcome back to our duplicates control training mini course this time we'll tell you about duplicate detection methods or plainly put how this function works behind the scenes there are a number of duplicate detection methods all handy in their own situations these methods can help you determine the best way for the system to detect duplicates in your case the big data method is as hand free as it gets ammo crm's own machine learning algorithm is you could say experienced and mature by now and can detect duplicates based on a whole variety of data inputs when it comes to duplicate incoming leads from chats the big data method is capable of discovering them as well as merging the new request with an existing lead next up is the tracking pixel with its help which is useful in more ways than one the system will check for the pixel footprints in the incoming lead and try to locate a similar footprint among your leads with this method the system will check the email address in the contact card attached to a new lead and try to locate a similar email that belongs to a similar contact in another lead it'll also match against the main contact to determine that matching the phone number is very similar to the email method except this time the phone number is matched against any other lead with the main contact that has a similar number and finally matching by a custom field you can pick custom fields of leads and contacts that will be matched against each other in order to locate a duplicate the system will check the data in these fields in the new lead or contact and seek similar information already in the system if the barriers are similar a duplicate is found that's all for duplicate detection methods each one is useful in its own way but if you're not sure which one is best for you opt for the big data method it'll sort your duplicates out no problem let's proceed to the next video and see how each of these methods works in action oh 