# How to import data to Kommo

so you've just started working with Como or perhaps when it was called ammo CRM and you have all your old warm leads inside spreadsheets and you're probably wondering how do I transfer them to the CRM as fast as possible hi there I'm Simon and today I'm going to teach you how to import lead data to Como in just a few clicks let's get started let's say for argument's sake that I'm a retro car workshop owner before my team started working with Como we've been collecting lead data in a good old-fashioned Excel spreadsheet now we need to transfer the data from the spreadsheet to Como so that it lands in the sales pipeline correctly this sounds like a challenge but with Como it doesn't have to be in just a few steps my lead base is successfully imported into the Como sales pipeline the lead cards are filled out automatically and my entire sales team can start doing their magic right away okay so let's take a step back now and see how you can prepare for a file import here is our spreadsheet with leads we'd like to import into Como let's start with the theory how do we prepare the spreadsheet so everything is imported correctly well please make sure that your file complies with the following requirements before you start if you're importing from a spreadsheet the file format should be XLS Excel SX ODS or CSV the uploaded file should not exceed two megabytes if your file size is more than two megabytes please break it down into several files and upload them separately if your file contains more than 10 000 lines break them into several files delete any empty lines or columns or the lines or columns that you're not planning to import it will make it easier for you to match the fields and no extra information will be uploaded make sure you only have one sheet in your document the system will import only the first sheet make sure all your columns in your file match the names in the fields in Como including the capitalization of the letters and the spelling otherwise you'll have to match the fields manually and finally if you upload leads check that you have a lead title in the file otherwise the file won't import now once you've prepared your spreadsheet let's start importing in Como you can choose one of the three import methods Number One Import from a spreadsheet number two import from Google Sheets or number three use Google Sheets integration let's take a look at each of them first let's learn how to import from a spreadsheet in the pipeline click on the three dots menu and select import the pop-up window will open here we select import a spreadsheet now you can either download an example or import your own file to upload your file click the load file button now after you do this the import settings window will appear you're off to a great start if you've done everything correctly and the headings match the fields in Como well done now we'll need to make some adjustments before we import the file let's begin by choosing a delimiter for your file first of all a delimiter is a character that marks the beginning or the end of a unit of data the system chooses it automatically depending on the format of your file for example CSV files are separated by a comma if the system doesn't choose the delimiter correctly make sure to switch it to the right one in case the delimiter is chosen incorrectly everything will be imported in one line now that's not something that you want so how do you define a delimiter of your file so that doesn't happen here is a list of the delimiters for the full supported file types if your file ends with CSV it's comma separated if it ends with XLS xlsx or ODS it's a semicolon separated now these are true most of the time but like every rule out there there can be exceptions sometimes there's CSV file can also be separated by a semicolon and vice versa now if you don't have any headings in your file uncheck the dot import first line contains field names Box by default the system starts importing it without the first line to avoid importing headings in our case though we'll leave it checked now we need to connect each spreadsheet con to a field in Como so that everything is imported correctly under each column in the import settings there is a coinciding Como field now one more thing by default all the uploaded data goes to the first stage of the first pipeline regardless of which pipeline you've imported the file from but you can change that by adding two extra columns to your file Pipeline and Lead status if a field doesn't match any of the fields in Como you will see a do not import warning and this column will not get transferred to Como let's check this out I have a field car brand in my coma account under the contact e that I forgot to name it correctly in Como and now it says do not import so how do I fix that first click on the select box below the column if you don't have a matching field in Como you can set it up on the go just select the new custom field option after you click on that option you'll be able to decide in which section you want to upload it contact company or lead then choose its type short text numeric toggle switch select Etc and rename it if needed just don't forget to save the settings when you're done now matching Fields can be a very tiring task we suggest naming all the headings the same as the fields in Como however if you did do that beforehand and matched all the fields manually you can save a template for your future Imports now with a template Como will automatically recognize the column names and match them to the correct fields Now to create a template just enter its name in the save template field we can also categorize the future imported leads with tags at this stage just click create tag and choose an existing one or create a new one but what if we choose to import from Google Sheets let's go back to our import window and try to import from Google Sheets this time to start the process we'll need to authorize our Google account then coma will request access to the Google account now let's press allow now that we're logged in time to choose which spreadsheet you want to upload once the spreadsheet is uploaded the import settings window will open same as before we need to match columns with all the fields in Como after matching all the fields we're ready to upload the data to Como press next to complete the import now the system will start importing the leads and the progress window will open when the import is finished the system displays its summary you can press show the imported data to see the detailed results of the input or simply press continue the CRM all right the last method of importing data to Como is Google Sheets integration now whether you manually enter data into your spreadsheets or collect Google forms entries automatically every new row in your Spreadsheet will automatically create a lead in your como account if you want to learn more about this integration you can check out our detailed video on our YouTube channel okay now let's look at how imported data will look in the pipeline once the import is complete the imported leads will land in the pipeline and all the data attached to the spreadsheet let's open one of the lead cards to check it out yes the lead name contact name email and sales values have been filled out automatically for us plus an automatic import tag has been added now this feature will help you to easily search for the data in Como after you import it pretty easy right now you know how to import lead data in Como in just a few simple steps okay that's all for now everybody if you've enjoyed this video please give it a like and don't forget to subscribe to see more tutorials like this one thank you very much indeed for watching and I'll see you in the next video thank you foreign 