# Skeleton CLI Masterclass | Kommo widget tutorial

in this video we'll show you how to use Skeleton the official CLI tool to scaffold comma widget projects It sets up everything you need React TypeScript localization deployment configs and more all in one command Skeleton saves time avoids config errors and lets you focus on building the logic of your widget Let's dive in The skeleton CLI is a command line tool to scaffold widget projects It automates creating the project structure with React TypeScript and other modern tools including deployment configs and documentation placeholders It sets up everything you need for a custom widget boilerplate code type definitions and localization support out of the box By using skeleton you save time on setup and reduce the chance of configuration bugs leading to a smoother development experience Next why you would use Skeleton como widget development often lacks clear boilerplate and documentation Skeleton addresses this by generating a standardized starting point It handles environment setup and deployment configs so you can focus on writing the widget logic instead of boilerplate Advantages: It accelerates development improves code quality by enforcing best practices and reduces errors It also lightens the support team's load since projects are consistent Next we'll walk through how skeleton works step by step Step one run npx como CRM create integration in your terminal Step two if not using flags pick a language English Spanish or Portuguese when prompted and also Indonesian and Turkish was recently added Step three enter the project directory name The CLI will create a folder for your widget Step four choose the local you need The tool will create I18N with translation JSON files Step five the CLI guides you through manifest setup Decide if the widget should initialize once or on every page load what data it needs links emails and which locations in the como app it will support It prompts for any extra required fields These interactive prompts ensure your widgets configuration and manifest are correctly set up Now we will see a full demonstration of Skeleton Works in real life Open a terminal and run Enro on the port where your app is listening Enro is a reverse proxy service that creates a public URL for your local development server enrock.com In the Enro console you will see a forwarding URL that tunnels to localhost 3000 Next generate the widget project scaffold using comos skeleton CLI Install and run the tool with a command like npx atcomm/createcomo integration This CLI available on npm and GitHub will prompt you step by step to configure a new integration For example it will ask you to pick your command line interface language In this case we will select English and then enter a project directory name For demo purposes you can press enter to skip optional fields like support website or support email and also select the languages your integration will support When it asks which comma locations your widget should appear in select all the ones you want This populates the locations array in the generated manifest.json For the demo you can enable any test utilities as prompted Once the scaffold is created the CLI will finish and display a summary of next steps Let's navigate into the newly created widget directory and open it in our editor In this case we use Visual Studio Code The project contains a readme.md with detailed setup instructions Inside you will find a client folder that holds the widgets front-end code HTML JavaScript CSS and image assets This code is what gets loaded into the Como interface as your widget Open Como in your browser and log into your technical account Click on create integration and in this time public Give your integration the widget code that is a unique identifier of the widget Grant the needed permissions to the integration and specify the supported languages In this case English and Spanish as we selected before In the redirect URL enter your Ingro tunnel address and we add the path widget off This is the endpoint comma we'll call when authorizing or using the widget In our case we used widget off as an example but the path can be anything that the developer defines on their back end To clarify this is a redirect to your backend For the demo we're using Enrock which points to our local host Then save the integration Comma will generate a client ID and client secret for you Keep these handy for the next step In your widget code locate the environment configuration files.dev dev.env for development andprod.env for production Populate these with your integrations details For example set integration year code with your widget code/ identifier in both files In base URL we selected the enro URL local host port or port the local port number where your front-end dev server will run For example in this case we will use the default value 9,000 These values let your widget code know where to reach your backend and identify which integration it is In the widgets manifest.json also update any web hook or frame URLs For example under ADP digital pipeline block you might set web hook URL with the endrock link Similarly update SMS or mobile as needed for the locations you selected In short wherever the widget needs to call back to your service use the Enro forwarding URL Next go into the server folder of your project that contains the backend code Rename or copyenv example to env Let's set client ID with the integration ID from como that you found under your integrations keys and scopes Client secret with the secret key from como Redirect URI with the same Enro URL plus widget O you used before Subdomain the subdomain of your Como account that can be easily found on the link of your profile This configures your server's OOTH settings so Como can redirect back during O To start your development environment first open a terminal Move into the client folder and run yanddev This spins up the front-end server on localhost 9000 and enables live reload Next open a second terminal Switch to the server folder and run yarndev Again this launches the backend API In our setup we specified port 3000 as the default but you can configure a different port if needed These commands work as long as both the client and server package.json files include a dev script which they do in a standard como scaffold To test your integration without re-uploading each time enable Como's dev mode In the browser console enter a local storage command that tells Commo to use your local server and next after it set item Now two values should be set The first one is the integration code of your integration Next to underscore is underscore dev and the second value should match your local host port This instructs como to load the widget from your local development server at localhost 9000 Now refresh the page Now build the widget bundle from the project route Run yarn build The yarn build command compiles the code for production It will generate a disc directory containing the final build artifacts and produce a widget zip file This archive packages all the widgets assets the JS manifest JS code CSS images and localization files exactly as Como expects Return to the Como integration settings in your browser And in my submission section select your integration and upload the widget zip as the widget bundle for your integration Choose a visible name for your widget on the marketplace Save changes and refresh At this point the integration is set up Install the integration for example by connecting it to a Como account and watch the logs If you search for your integration in the browser you will be able to install it and use it When the integration is installed comma will hit your widget off endpoint to authorize You should see a log like get widget off 200 Okay in your server console This 200 okay status code means comos request succeeded and your handler returned success Now test the actual widget functionality For example let's go to leads and on the pipeline let's trigger our salesbot that we have pre-built by moving one lead from stage If we go to our chat section we will see how the salesbot sent us a message In the console you should see something like post web hook salesbot 200 Okay this indicates that comos salesbot posted data to your server and you responded successfully As we can see in the other terminal in the same way if we move again our lead to another stage of the pipeline we will see in the terminal how the data is successfully posted in the server and responded successfully With these steps your KMO widget integration is fully set up and testable You can now develop your widgets functionality knowing it is properly connected end to end I hope you enjoyed this tutorial I'd like to invite you to join our Discord server where you can ask questions about Skeleton and many other topics as well as connect with other COMO users in our community You can join by scanning the QR code or using the link below You'll also find links to Como our GitHub repository and our npm package 