# Widget Development

hello and welcome to the KO widget development webinar today we will discuss developing a simple widget without a backend the widget is available on GitHub and you can find it by clicking the link in the description today we are planning to Define what widgets are and their role within the Como platform we hope to help you understand the structure and specific requirements needed for a KO widget project and we'll show you a birthday widget which is available on our account in GitHub as an example the first thing we should do is Define what a widget in terms of Como is a widget is a small functional application or tool that can be integrated into the Kom platform to extend its capabilities and enhance its functionality before you start working on a widget there are several things you need to know and explore we will look at each of them the first one is the Como account register an account on Como to create an integration and upload a widget you can use any type of account trial SL technical SL compan account to upload and test your widget but we recommend using a trial account for testing your integration to develop for Como users you should be an experienced user of Como it takes some time to understand how the system works and how different objects interact with each other if the only thing you know about KO is its name we highly recommend spending some time exploring it you should be familiar with vs code or another code editor Developers can use code editors to build edit and debug code after which one can easily publish the app they are building some knowledge of CSS and HTML is expected as a widget is the visual part of an integration they are fundamental to web development with HTML providing the basic framework and CSS adding styling elements the script.js file is written in Java JavaScript to develop a widget you should have a good understanding of JavaScript you must know JavaScript Concepts such as variables functions objects arrays and Dom manipulation additionally knowledge of event handling asynchronous programming and Ajax will be beneficial for creating interactive and dynamic widgets jQuery is a JavaScript library designed to simplify HTML Dom tree traversal and manipulation event handling CSS animations and Ajax make sure you're familiar with twig twig is a templating engine designed for generating HTML it is primarily used with PHP but we use it in conjunction with JavaScript to generate HTML on the client side instead of on the server in Ko a widget looks like an archive of files some of the files are mandatory and others add functionality and design let's have a closer look at each component of the widget manifest.json is a widget's main file it links localization files images and files with JS a widget is represented as an object containing properties and methods that are useful to work with when the system loads widgets it it adds the functionality described in script.js to the existing system widget object this means that the custom widget object inherit properties and methods that will be helpful for further work the JavaScript portion of the widget contains the required components and additional functions it also includes callback functions that are triggered under specific conditions with the script.js you have a simple tool for inter interacting with the Dom and Performing cross domain requests you can use it to create basic text widgets generate information blocks from external data or send data to external Services these changes will work immediately for all users of your account I internalization is the folder where you add localization files if you want your widget to be available in three languages add three localization files en n. Json es. Json pt. Json remember that they should have the same structure if you have more than one localization templates are twig files that you can use in your widget you can use templates to create forms and buttons even if your widget isn't explicitly displayed anywhere except for the integration section you must still upload the images folder the folder contains five logos of different sizes but they can only be in one format PNG the CSS file contains styles for the widget to ensure the widget does not clash with other system elements and widgets its CSS file should contain unique class names for all Main and child elements when we were planning this tutorial we wanted something that would show the interaction between different fields including custom Fields use environment variables let us add templates show how to perform a click when a user clicks on place in the system and give users different rights so we decided on a birthday Widget the widget was made by the company freus after you install the widget you'll see the settings section the first one is the birthday field you can choose one of the existing fields or create a new new one the newly created field will have the type of birthday you need to type the name and choose a profile to create the field you can also choose the type of task and type in the text added to the task after creating a client's birthday then you can choose users responsible for sending wishes to clients and get those tasks going next there is a section that allows you to create chat templates then we navigate to the lead profile and see the widget displayed on the right panel if we click it now before we set the birthday for this lead we'll see that none of the clients are celebrating a birthday today if we set the birthday field to the current date we get the task and can click the button send a birthday they wish on the right panel once we press the button the list of templates will be open so you can choose a template from the template list let's have a look at each element of the birthday widget we will begin with manifest.json here we see the widget name and description will be taken from localization files here you pass the version of your widget but since you don't need to update a private widget you can pass any version here interface uncore version should always be set to two the inore once method is responsible for widget initialization if there is no common context for all the pages you should pass false since the widget we work on isn't VoIP we set in Itor once to false since we created a widget that can be seen when switching languages we mentioned all the languages we support en n stands for English PT stands for Portuguese es stands for Spanish we set installation to true as we have yet to set up our settings usually we set installation to false if the settings are managed in another system that interacts with Como via API locations field defines the areas in which the widget will initialize settings widget installation and configuration page card one the widget will be displayed on the right side panel of a lead profile see card zero the widget will initialize in a contact profile but won't appear on the right side panel we provide three images in every language for the widget slideshow widget. tore description will be pulled from I internalization files since we have different input types we create custom settings all the fields will be pulled from I internalization files we should add all the images the widget will use to the images folder all of them serve different purposes logos are images that are displayed in the areas of widget initializations all five should be added to the folder otherwise you will get a system error message keep in mind that logo images should be in PNG format slideshow images are shown on the installation screen to show your integration in action they aren't mandatory for private integration if your widget supports different languages you should provide slideshow images in all the languages other images in SVG format they are supposed to enhance the widget inter interface by making it more intuitive every line and word in the widget should be translated into all the languages in which your widget will be used the structure should be the same in all files for the script.js file not to be too long we divided some parts of it into classes this way it's easier to read edit and manage it just remember to import those classes at the beginning of the script.js file what happens in the cache. JS file when a cache object is created it checks to see if a cash object already exists in the KO widget property of the window object if the cash object does not exist an empty cache object is created next let's have a look at cache methods the get item method retrieves a value from the cache based on a given key it checks if the value for the key exists in the cach object in the Kom widget if it exists it is returned if the value is not in the cache the method checks session storage if the value is found in session storage and has not expired it is retrieved and returned if the value has expired the value is removed from session storage otherwise null is returned the set item method is used used to set a value to the cache if local iscore truecore the value is stored directly in the cache object in the Como widget otherwise the value is stored in session storage in both cases the value is serialized to Json and stored along the expiration period value the remove item method removes a value from session storage based on a given key the next one is the http.get options if the cash option is specified in the parameters an attempt is made to retrieve data from the cache if no data is in the cache and Ajax request is executed next we will examine the koa.js file the first step is to import http.get account obtains account information the method get tasks retrieves tasks with the given filter the method create tasks sends a post request to the API V4 tasks and point passing in the new task data in the body the method create field sends a post request to the custom Fields endpoint passing the data of the next custom field the method create a template sends a post request to the Ajax V1 chats templates add endpoint passing the new template data in the request body the method get templates sends a get request to the Ajax V4 chats templates endpoint the method get users sends a get request to the API V4 users Ando pushing all users into a users array the method get task types calls the get account method to get account information and then extract task types from the retri R data the method get Fields sends a get request to the custom Fields endpoint collecting all the fields into a field array the method get empty promise is used in the next function if there is nothing to return it calls get empty promise the method get Fields by type is used to get custom fields of specific types it sends queries to retrieve fields for various entity types contact leads and returns an array of field objects that match the given criteria next we will examine the events. JS file first we import the library moment and module lib SL components SLB slm the method settings process events such as clicking on the link to create a custom field for the widget and the template creation button this method get birthday info retrieves birthday information from the current page it checks if any contact or lead has a birthday today and Returns the current date the method card checks if today's date matches the birthday if it does and specific conditions are met it calls a method to create a new task if today is the birthday and no tasks have been created this method create task will create a new task using the birthday information and task parameters this is the load. JS file the methods prepend and append add HTML loader content before and after the specified element the method get HTML gets the HTML content from the loader if the HTML has not yet been generated the method will will render the loader twig template the methods hide show and remove access elements with the ko- loader class and hide show or remove them the method display save button displays a save button in a modal window with the specified code class this is settings. JS the method save is used for saving widget settings it handles the event and returns a promise inside the method it checks the widget status gathers data from the settings form creates an object with information about the widget State and handles the case when the widget is installed after processing the method Returns the data object upon successful saving the method load is used for loading widget settings it returns a promise and performs preliminary Ary configuration of widget parameters such as params dot template the classes template. JS used for rendering templates is a wrapper around the widgets own this. render methods this is intended to simplify the code but it doesn't mean you have to use this specific implementation you can stick to the basics or Create Your solution what is provided here is an example that can be improved first we import libraries J query Twigs JS text and a module lib SL components SLB slm modal the method flush text plug-in is used to temporarily change the settings for loading text files so that they are always loaded via xhr a jsapi to create http requ ests the method restore text plug-in is used to restore the default settings for loading text files after a temporary change the method check registry is used to check the existence of a twig template the method get from registry is used to retrieve a twig template by its name the method preload is used for pre-loading templates and CSS files it returns a promised that resolves after all resources have been loaded the method load templates is used for loading templates this method utilizes ap. widgets. system. area to determine the current area within the application it returns a promise that resolves after all templates have been loaded the method add template is used to add a template to Twig if a template with the given name already exists in the registry it is updated otherwise a new template is created the method load CSS is used for loading CSS files it returns a promise that resolves after all CSS files have been loaded the method render is used for rendering a template by its name and passing parameters to it the method install placeholder is used to install a placeholder in a specific location on the page as the wrap div argument indicates the exception argument can specify an element that should be excluded from the placeholder installation the getter get twig returns an object with methods for generating various user interface elements based on twig templates from KO then let's have a look at the plugins folder there is just one file in this folder this plug-in is designed for serializing form dat data into Json format it allows converting user entered form data into Json when you open the assets folder you will see two other folders CSS and templates we have two CSS files in the folder ko. CSS and style.css for several reasons code organization separating styles for different parts of the code reusability depend dependency management and Page load optimization here we can see how the display of a tool tip when hovering over an image is implemented there is a folder with templates The loader. Twig represents a loader block displayed on the page while content is loading or asynchronous operations are being performed the widget WR panel. twig showcases a widget in the right side panel it takes two parameters text which will vary based on whether it is a person's birthday that day and a button only displayed if there is a person's birthday The bass. Twig displays the widget's basic settings The model. Twig displays a settings modal window we won't go through the whole script.js file but we focus on some parts we start by importing all the classes a drop-down list is added where responsible users users are added when a checkbox is clicked upon clicking the button send a birth they wish a list of templates opens which can be sent to the chat be aware that the button works correctly if chats are already open the functionality does not include switching to a chat from other states that's it with the code you need to create an integration to make your widget work with Como then a upload your widget in zip format and add all the necessary languages before you upload the archive with the widget please ensure all the files are at the root level of an archive that's all we wanted to share for today thank you so much for joining us 