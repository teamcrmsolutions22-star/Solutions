# Planfix — REST API: Технічні деталі

## Автентифікація

- **Протокол:** OAuth 2.0 (Bearer Token) ✅
- **Генерація токена:** «Керування акаунтом» → «Доступ до API» → «REST API»
- **Заголовок:** `Authorization: Bearer <access_token>`
- **Увага:** токен успадковує права того співробітника, який його створив — для інтеграцій рекомендується обліковий запис робота з максимальним доступом

## Базова структура запитів

| Параметр | Значення |
|----------|---------|
| Base URL | `https://<account>.planfix.com/rest/` або `https://<account>.planfix.ua/rest/` |
| Формат відповіді | JSON (метадані + масив результатів) |
| Пагінація | `limit` + `offset`, максимум **100 записів** за запит ✅ |
| Фільтрація | Об'єкт фільтрації у тілі POST-запиту (напр. `/task/list`) |
| Silent mode | `?silent=true` — без сповіщень і записів в історії ✅ |

## Rate Limits ✅

| Тип | Значення |
|-----|---------|
| Секундний ліміт | **1 запит/секунду** |
| Добовий ліміт | **20 000 / 50 000 / 100 000** (залежно від тарифу) |
| Free тариф | API повністю заблоковано |
| Заголовок контролю | `X-RateLimit-Remaining` (оновлюється кожні 10 хв) |
| При перевищенні | HTTP 429, код помилки 22 |

## Ключові ендпоінти

### Задачі
```
POST   /task              # створення задачі
GET    /task/{id}         # отримання задачі
POST   /task/{id}         # оновлення задачі
POST   /task              # підзадача: передати parent: {id: <parent_id>}
```

### Проєкти
```
POST   /project           # створення
POST   /project/list      # список
```

### Контакти
```
POST   /contact           # створення
POST   /contact/list      # пошук
```

### Коментарі
```
POST   /task/{id}/comments   # додавання коментаря
DELETE /comment/{id}         # видалення
```

### Файли
```
POST   /file              # завантаження (multipart/form-data) ✅
```
> Максимальний розмір файлу не задокументований ⚠️; обмежений дисковим простором акаунта

### Довідники
```
POST   /directory/{id}/entries   # отримання записів ⚠️
```

### Кастомні поля
```
GET    /customfield/task      # метадані полів задач
GET    /customfield/contact   # метадані полів контактів
GET    /customfield/project   # метадані полів проектів
```

## Пакетні операції

Нативна підтримка bulk-запитів **не задокументована** ⚠️  
→ Використовувати послідовні запити з throttling (1 запит/сек)

## Коди помилок

```json
{
  "error_code": 1,
  "error_message": "Invalid token"
}
```

| Код | Опис |
|-----|------|
| **1** | Невалідний токен |
| **6** | Доступ до методу/об'єкта заблоковано |
| **21** | API на безкоштовному тарифі (HTTP 403) |
| **22** | Перевищено добовий ліміт (HTTP 429) |
| **30** | Помилка парсингу JSON |
| **1000** | Задача не існує |

## HTTP 202 — Паралельне оновлення

Якщо задача оновлюється паралельно іншим сценарієм → HTTP 202  
**Це нормальна логіка Planfix** — запит стає в чергу автоматично, додаткових дій не потрібно ✅

---

## Золоте правило API всередині ПФ

API-запити всередині ПФ допустимі, але повинні змінювати об'єкти, які **не можна** змінювати сценаріями:
- записи довідників
- співробітники і групи
- окремі рядки аналітик

⚠️ **Не можна** звертатись по API із задачі до тієї ж задачі.

---

## Повний список ендпоінтів

```
GET    /task/list                    # список задач (з фільтрами)
POST   /task/                        # створити задачу
GET    /task/{id}                    # отримати задачу
POST   /task/{id}                    # змінити задачу
GET    /task/{id}/checklist/list     # чек-ліст задачі
POST   /task/{id}/checklist/         # створити пункт чек-листа

GET    /contact/{id}                 # контакт
POST   /contact/{id}                 # змінити контакт
GET    /contact/list                 # список контактів

GET    /user/{id}                    # співробітник
POST   /user/{id}                    # змінити (timezone, workspace, керівники)

GET    /directory/{id}/entry/list    # записи довідника
POST   /directory/{id}/entry/        # створити запис
PUT    /directory/{id}/entry/{key}   # оновити запис
POST   /directory/{id}/group/        # створити групу в довіднику (з січня 2026)

POST   /file/                        # завантажити файл (targetType: task/directory)

GET    /report/{id}/data             # дані звіту (тільки Корпорація, 1 раз/10 хв)

GET    /workspace/list               # список робочих просторів (з грудня 2025)
```

`totalCount` у відповіді — повна кількість без обмеження pageSize.

### XML API (застарілий)
- Basic Auth: API Key + session key від `auth`
- Офіційно не deprecated, але пріоритет — REST

---

## JSONARRAY — масиви аналітик у HTTP-запиті

```
"data": [
%%$ДЛЯКОЖНОГО({{Аналітика.Специфікація}};",")$%%
  {
    "field1": "{{Аналітика.Специфікація.Поле1}}",
    "field2": "{{Аналітика.Специфікація.Поле2}}"
  }
%%$КІНЕЦЬБЛОКУ$%%
]
```

---

## Відомі обмеження і баги

| Проблема | Деталі |
|---------|--------|
| Аудитори задачі через API | Отримати не можна — тільки аудиторів проекту |
| Пошук задачі за числом | Якщо в назві іншої задачі є таке ж число — пріоритет назві (баг, лютий 2026) |
| Багаторядкові рядки | Передавати як `<p>TEXT</p>` |
| Відповідальний контакту | Поле `assignee` в `/contact/{id}` (не задокументовано явно) |
| Авто-нумерація через вебхук | Якщо передати поле явно — авто-нумерація не спрацює |

---

## Як дізнатися ID поля

| Спосіб | Де | Складність |
|--------|-----|-----------|
| Розширення Тахіра | t.me/planfix_com2/28069/28071 (MIT) | ⭐ Просто |
| Через URL | Управління акаунтом → Поля → Редагувати → число в URL | ⭐⭐ |
| Через API | GET `/task/field/list` або `/contact/field/list` | ⭐⭐⭐ |

## Інструменти екосистеми

| Інструмент | Що робить |
|-----------|----------|
| Planfix Tweaker (Chrome, Тахір Біккінін) | Підсвітка синтаксису формул |
| Розширення ID полів | ID полів одним кліком |
| PowerShell-скрипт (github.com/kirei4ik/planfix_getreport) | Вивантаження звітів без прав адміна |
| Swagger → Postman (help.planfix.com/restapidocs/swagger.json) | Швидкий старт з API |
