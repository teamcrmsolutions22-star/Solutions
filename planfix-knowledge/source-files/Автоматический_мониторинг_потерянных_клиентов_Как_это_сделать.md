# Автоматический мониторинг потерянных клиентов | Как это сделать

в этом видео поговорим о работе с клиентами в планфикс вы узнаете как упростить взаимодействие и автоматически отслеживать их активность рассмотрим автоматизацию на примере цветочного магазина когда у магазина появляется новый клиент добавляем его в список новые клиенты и заполняем данными его карточку чтобы быстро создать заявку соответствующий список Обратите внимание в задаче новый клиент автоматически стал контрагентом заполняем остальные поля и сохраняем заявку заметьте что в карточке клиента количество активных заявок увеличилось на единицу далее заявка идёт в работу и проходит все этапы Воронки продаж когда заявка становится завершённый счётчик активных задач в карточке клиента обнуляется с этого момента планфикс будет отслеживать отсутствие новых заявок от клиента если он не сделает новую заявку в течение 30 дней карточка автоматически переместится в список Потерянные клиенты такая автоматизация В перспективе может помочь при создании программ лояльности для поддержания интереса клиентов к товарам или услугам далее поговорим о настройках такой автоматизации переходим в объекты и создаём заявку Для нашего примера достаточно заполнить поле названия создаём объект и переходим к набору статусов он описывает жизненный цикл заявки добавим в набор нужные статусы если стандартный статус нам подходит для бизнес-процесса используем его таким же способом добавляем статусы ожидания оплаты и оплата получена когда набор готов создаём Канбан по статусам он будет нашим планировщиком для работы с клиентами в разделе контакты переходим во вкладку шаблоны открываем на редактирование шаблон контакта добавляем поле типа число для отслеживания количества активных заявок затем добавляем поле типа список для указания статуса клиента задаём значение например новый активный потерянный Не забудьте сохранить настройки на последнем шаге для работы автоматизации создадим три автоматических сценария первый и второй будут менять значение счётчика в карточке клиента а третий - его статус Давайте настроим сценарий который будет увеличивать количество заявок в карточке клиента переходим в автоматические сценарии заявки создаём новый сценарий указываем название события и условия для срабатывания в блоке операций выбираем изменить контакт В текущей задаче выбираем поле контрагент далее указываем что мы хотим вычислить значение Поля с названием количество активных заявок и добавляем в него такую формулу для вычисления меняем значение поля статус клиента на активный и сохраняем сценарий следующий сценарий будет уменьшать количество активных заявок в карточке клиента добавляем к заявке ещё один сценарий указываем события для срабатывания и условия сценария в блоке операций указываем что В текущей задаче хотим изменить поле контрагент вычисляем значение поля количество активных заявок с помощью такой формулы сохраняем настройки далее создам последний сценарий который будет переводить клиента из статуса актив для этого переходим в процессы контактов и выбираем нужный процесс в нём создаём новый автоматический сценарий указываем название события и условия для срабатывания в очереди на выполнение ставим интервал 30 дней без учёта рабочего времени ответственных заполняем поле выполнить после наступления указанного времени и включаем функцию новое срабатывание сценария в задачи начинает отсчёт заново Далее в блоке операций указываем изменить этот контакт установить значение поля статус клиента потерянный сохраняем сценарий Теперь если в течение 30 дней Клиент не сделает новый заказ его карточка будет автоматически переведена в статус потерянный на этом всё Обратите внимание для автоматизации мы использовали только базовые настройки план фикса Используйте этот кейс чтобы сделать работу с клиентами проще и приятнее 